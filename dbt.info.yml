name: [DBT] Drupal Base theme
type: theme
description: Drupal Base theme makes a theme development easier.
core: 8.x

# Using "Classy" as the base theme to avoid unnecessary dependencies
base theme: classy
